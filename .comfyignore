# 发布到 Comfy Registry 时要排除的文件。语法和 .gitignore 一样。
# 默认行为是「打包所有被 git 跟踪的文件」，所以得在这儿点名排除。

# 开发机的作业规程：里面有本机绝对路径（ComfyUI 装在哪、插件怎么挂进来），
# 不该跟着节点包发给用户。
AGENTS.md

# 测试套件：开发时才跑，用户用不到。跑起来还依赖开发机上的 Python 环境。
tests/

# 打包自己的产物和规则文件
.comfyignore
*.zip
